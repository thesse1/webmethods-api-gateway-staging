variables:
  foo: bar

steps:
- bash: |
    echo "This pipeline runs first"
    echo
    echo "Variable foo:"
    echo $(foo)
    echo
    echo "Directory:"
    ls -l
    echo
    echo "Swagger:"
    cat petstore_swagger_updated.json

- publish: petstore_swagger_updated.json